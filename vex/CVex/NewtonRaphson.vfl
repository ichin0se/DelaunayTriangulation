cvex NewtonRaphson(int i = 0; export float rval = 2){
	if(i > 1){
		float prev;
		NewtonRaphson("i", i - 1, "rval", prev);
		rval = 0.5 * (prev + (2.0 / prev));
	}else
		rval = 2;

	printf("%d: %f\n", i, rval);
}
